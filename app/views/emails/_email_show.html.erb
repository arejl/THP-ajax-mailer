<h3>Objet : <%= @email.object %></h3>
<h3>Contenu du message :</h3>
<p><%= @email.body %></p>
<div class="d-flex">
  <%= link_to "Supprimer", email_path(id: email.id), method: "delete", class: "btn btn-danger mx-2", remote: true %>
  <%= link_to "Marquer comme non lu", email_path(id: email.id), method: "patch", class: "btn btn-secondary mx-2", remote: true %>
</div>