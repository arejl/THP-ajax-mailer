<% if email.read == false %>
  <%= link_to email.object, email_path(id: email.id), method: "get", class: "btn btn-link p-0 font-weight-bold", remote: true %>
<% else %>
  <%= link_to email.object, email_path(id: email.id), method: "get", class: "btn btn-link p-0", remote: true %>
<% end %>

